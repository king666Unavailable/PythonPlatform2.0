# 公共组件目录

按复用范围组织 UI：

- `ui/`：页面标题、状态标签、指标卡等基础控件。
- `navigation/`：图标和导航相关组件。
- `data-display/`：题目浏览、表格和列表展示。
- `feedback/`：加载、错误、成功和空状态。
- `../layouts/`：应用壳和角色导航，不放具体业务页面。

业务页面通过组合公共组件完成任务流程，不能把 API 返回对象直接渲染成 JSON。
